/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: "#F57F17", // Matches PALETTE.Primary.Orange
          light: "#FDA84D",   // Matches PALETTE.Primary.LightOrange
        },
        secondary: {
          DEFAULT: "#F4EDE4", // Matches PALETTE.Secondary.Beige
          light: "#FFFDF7",   // Matches PALETTE.Secondary.Cream
          pale: "#FBF4E4",    // Matches PALETTE.Secondary.Pale
        },
        accent: {
          green: "#43A047",     // Matches PALETTE.Accents.Green
          "green-dark": "#3A7F56", // Matches PALETTE.Accents.DarkGreen
          teal: "#58766A",      // Matches PALETTE.Accents.Teal
          rust: "#B75F4C",      // Matches PALETTE.Accents.Rust
          yellow: "#FFD54F",    // Matches PALETTE.Accents.Yellow
        },
        vo: {
          white: "#FFFFFF",
          black: "#212121",
          gray: "#4E4E4E",
          bg: "#F4EDE4",       // Matches PALETTE.Secondary.Beige (standard bg)
          card: "#FFFDF7",     // Matches PALETTE.Secondary.Cream
          "text-primary": "#212121",
          "text-inverse": "#FFFDF7",
          "text-muted": "#4E4E4E",
        }
      },
      fontFamily: {
        primary: ["'42dot Sans'", "sans-serif"],
        body: ["'42dot Sans'", "sans-serif"],
        heading: ["Lora", "serif"],
      },
      spacing: {
        'vo-space-4': '1rem',
        'vo-space-5': '1.25rem',
        'vo-space-10': '2.5rem',
      },
      borderRadius: {
        'vo-radius-base': '0.5rem',
        'vo-radius-md': '0.75rem',
        'vo-radius-xl': '1.5rem',
        'vo-radius-2xl': '2rem',
      },
      zIndex: {
        'vo-sticky': '10',
        'vo-fixed': '50',
        'vo-dropdown': '40',
        'vo-modal': '100',
      }
    },
  },
  plugins: [],
}
